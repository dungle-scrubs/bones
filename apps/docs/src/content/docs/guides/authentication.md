---
title: Authentication
description: Configure LLM API access for Bones agents.
---

Bones agents need access to an LLM API. Set the `ANTHROPIC_API_KEY`
environment variable:

```bash
export ANTHROPIC_API_KEY=sk-ant-...
```

Each agent turn is billed at standard Anthropic API rates.
